# Legacy SQL Reference

Files in this directory are legacy/manual SQL helpers kept for reference.

- They are not the canonical migration history.
- New schema changes should be added under `database/migration_*.sql`.
- When bootstrapping or reconciling an environment, prefer the numbered migrations in [database/README.md](../../database/README.md).

